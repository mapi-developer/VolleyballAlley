import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["smelting-helpline-botanist.ngrok-free.dev", "https://cyan-carrots-win.loca.lt"],
};

export default nextConfig;